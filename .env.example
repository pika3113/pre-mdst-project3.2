# Environment Variables for Wordle with Authentication
# Copy this file to .env and fill in your actual values

# JWT Secret Key - Generate a secure random string
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SECRET_KEY=your-very-secure-jwt-secret-key-change-this-in-production

# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Redirect URI (should match what you set in Google Console)
# For development (Docker):
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback
# For production, change to your domain:
# GOOGLE_REDIRECT_URI=https://yourdomain.com/auth/google/callback

# Database path (optional, defaults to wordle_stats.db in container)
# DATABASE_PATH=/app/data/wordle_stats.db
